@using ELake.Controllers
@using Microsoft.Extensions.Localization
@inject IStringLocalizer<SharedResources> SharedLocalizer
@{
    ViewData["Title"] = SharedLocalizer["UnpublishGeoTIFFWater"];
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<body class="bg-light">
    <div class="container">
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h2 class="pb-3 mb-1 font-italic border-bottom">@SharedLocalizer["UnpublishGeoTIFFWater"]</h2>
                <p class="lead">There should be a description</p>
                @Html.ActionLink(SharedLocalizer["Instruction"], "Instruction", new { Type = "PublishGeoTIFF" })
                @if (!string.IsNullOrEmpty(ViewBag.Message))
                {
                    <div>
                        @ViewBag.Message
                    </div>
                }
            </div>
        </div>
        <div class="form-row ">
            <div class="col-md-4">
                <form asp-action="UnpublishGeoTIFFWater">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label class="control-label">@SharedLocalizer["Type"]</label>
                        <select id="Type" name="Type" class="form-control" asp-items="ViewBag.Type" onchange="ChangeType()"></select>
                    </div>
                    <div class="form-group">
                        <label class="control-label">@SharedLocalizer["Folder"]</label>
                        <select id="Folder" name="Folder" class="form-control" asp-items="ViewBag.Folder"></select>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="@SharedLocalizer["Unpublish"]" class="btn btn-outline-primary my-2 my-sm-0" />
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>
@*<div class="row">
    <div class="col-md-4">
        <form asp-action="UnpublishGeoTIFFWater">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label class="control-label">@SharedLocalizer["Type"]</label>
                <select id="Type" name="Type" class="form-control" asp-items="ViewBag.Type" onchange="ChangeType()"></select>
            </div>
            <div class="form-group">
                <label class="control-label">@SharedLocalizer["Folder"]</label>
                <select id="Folder" name="Folder" class="form-control" asp-items="ViewBag.Folder"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="@SharedLocalizer["Unpublish"]" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>*@

<script>
    function ChangeType() {
        $.ajax({
            url: '@Url.Action("GetWaterDates")',
            data: {
                Type: $('#Type').val()
            },
            type: 'POST',
            success: function (data) {
                var optionhtml = '';
                $.each(data, function (i) {
                    optionhtml += '<option value="' + data[i] + '"' +
                        '>' + data[i] +
                        '</option>';
                });
                $("#Folder").empty();
                $('#Folder').append(optionhtml);
            },
            error: function () {
            }
        });
    }

    $(document).ready(function () {
        ChangeType();
    });
</script>